useDynLib(batchmix)

importFrom(ggplot2,
  aes,
  facet_grid,
  facet_wrap,
  geom_boxplot,
  geom_line,
  geom_point,
  ggplot,
  label_both,
  labeller,
  labs
)

importFrom(stats, 
  cutree,
  as.dist,
  hclust,
  median,
  rbeta,
  rchisq,
  rpois,
  rnorm
)

importFrom(tidyr, 
  any_of,
  contains, 
  pivot_longer
)

importFrom(Rcpp, evalCpp, sourceCpp)

export(
  assessConvergence,
  calcAllocProb,
  collectAcceptanceRates,
  continueChain,
  continueChains,
  createSimilarityMat,
  generateBatchData,
  generateBatchDataLogPoisson,
  generateBatchDataMVT,
  generateBatchDataVaryingRepresentation,
  generateInitialLabels,
  getBestChain,
  getLikelihood,
  getSampledBatchScale,
  getSampledBatchShift,
  getSampledClusterMeans,
  minVI,
  plotAcceptanceRates,
  plotBatchCorrection,
  plotLikelihoods,
  plotPredictiveCheck,
  plotSampledBatchMeans,
  plotSampledBatchScales,
  plotSampledClusterMeans,
  plotSampledParameter,
  predictClass,
  predictFromMultipleChains,
  processMCMCChain,
  processMCMCChains,
  rankNormalizedRhat,
  rStickBreakingPrior,
  runBatchMix,
  runMCMCChains,
  samplePriorLabels,
  simulatePosteriorPredictive,
  simulatePriorPredictive
)

exportPattern("^[[:alpha:]]+")

# S3 methods for the classes fitBatchMix()/runBatchMix()/
# batchSemiSupervisedMixtureModel()/assessConvergence() attach to their
# return values (see R/batchmixFitMethods.R) - registered explicitly
# (rather than relying on exportPattern() above alone) so dispatch on
# print()/summary()/format() works correctly from other packages/contexts,
# not just at the top-level prompt with batchmix attached.
S3method(print, batchmix_fit)
S3method(summary, batchmix_fit)
S3method(print, summary.batchmix_fit)
S3method(print, batchmix_fit_list)
S3method(summary, batchmix_fit_list)
S3method(print, summary.batchmix_fit_list)
S3method(format, batchmix_convergence)
S3method(print, batchmix_convergence)
S3method(print, batchmix_control)
